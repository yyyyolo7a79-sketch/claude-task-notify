# Claude Code 任务完成弹窗 — 原理、文件路径与实现结构

> 用途：供外部审查（GPT/人工）检查潜在问题。本文档描述**当前生效实现**的全部文件绝对路径、技术原理、数据结构与已知边界。审查时请关注：编码/转义、并发、安全（注入/隐私）、DPI/多显示器、hook 阻塞与超时、异常兜底。

---

## 一、环境信息

| 项目 | 值 |
|---|---|
| OS | Windows 11（Home China，10.0.26200），显示缩放 150%（物理分辨率 2560×1600） |
| PowerShell | 5.1（`C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe`），默认非 STA、非 DPI aware |
| Claude Code | v2.1.220（Windows 原生） |
| 模型 API | DeepSeek（`ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic`，见 settings.json env） |
| 运行形态 | VS Code 扩展内运行 Claude Code；hook 进程由 Claude Code 直接派生 |

---

## 二、文件清单（绝对路径）

### 2.1 生效文件（全局，所有项目生效）

| 文件 | 作用 | 关键点 |
|---|---|---|
> 以下路径以安装用户为准，通用占位符 `C:\Users\<你的用户名>\` 等价 `~\.claude\`。

| 文件 | 作用 | 关键点 |
|---|---|---|
| `C:\Users\<你的用户名>\.claude\settings.json` | 全局配置：模型、env、statusLine 插件、**hooks.Stop** | hooks 节见 §4.1；另有备份 `settings.json.bak`（无 hooks 的原始版） |
| `C:\Users\<你的用户名>\.claude\scripts\notify-complete.ps1` | **hook 入口**：过滤/摘要/去重/派生 UI，500ms 内返回 | UTF-8 BOM；读 stdin JSON |
| `C:\Users\<你的用户名>\.claude\scripts\show-popup.ps1` | **UI 进程**：WPF 卡片弹窗，独立运行，约 10 秒自关（8s hold + 淡入淡出） | UTF-8 BOM；`-STA` 启动（入口派生时内置） |
| `C:\Users\<你的用户名>\.claude\skills\claude-task-notify\SKILL.md` | 行为规范 skill：定义 Claude 收尾格式（`✅ 已完成：…` / `❓ 需要你提供：…`） | 非强制层，与 hook 判断互相印证 |
| `C:\Users\<你的用户名>\.claude\CLAUDE.md` | 全局工作流规范；末尾含「任务完成弹窗（全局强制）」小节 | 声明 hook 已配置 |

### 2.2 运行时临时文件（`%TEMP%\claude-code-notify\`，即 `C:\Users\<你的用户名>\AppData\Local\Temp\claude-code-notify\`）

| 文件 | 用途 | 生命周期 |
|---|---|---|
| `payload-<guid>.json` | 入口→UI 传递弹窗内容 | UI 读取后立即删除；>10 分钟未读由入口下次执行时清理 |
| `state.json` | 去重状态（key + 时间戳） | 每次触发原子替换（写 .tmp 后 Move-Item） |
| `notify.log` | 脱敏诊断日志 | 入口/UI 共写同一文件，命名 Mutex 串行化；>200KB 时截断（SetLength(0)，不删除） |

### 2.3 仓库（GitHub）

`<本仓库本地克隆目录>\`（路径因人而异，以下为仓库内文件清单）

| 文件 | 说明 |
|---|---|
| `README.md` | 项目说明（安装/卸载/参数/FAQ） |
| `原理以及文件路径.md` | **本文档**（第五轮审查后移出 `需求/` 入库，不再被忽略） |
| `scripts\notify-complete.ps1`、`scripts\show-popup.ps1` | 与全局同版本副本（SHA-256 校验一致） |
| `skills\claude-task-notify\SKILL.md` | skill 副本 |
| `弹窗参数调整器.html` | 可视化参数调整工具（浏览器打开，按 系统缩放×浏览器缩放 物理像素校准预览） |
| `需求\需求.md`、`需求\claude-code-completion-popup-spec.md` | 原始需求与规格文档（`.gitignore` 忽略 `需求/`，不入库） |
| `LICENSE`（MIT）、`.gitignore` | — |

---

## 三、技术原理

### 3.1 架构总览

```
Claude Code 主 Agent 完成本轮回复
        │
        ▼ ① harness 层触发（强制，模型无法跳过）
Stop hook（settings.json → notify-complete.ps1，timeout 5s）
        │ ② stdin JSON（UTF-8，字段见 §3.2）
        ▼
notify-complete.ps1（入口进程，目标 500ms 内 exit 0）
        │ ③ 过滤：仅 hook_event_name="Stop" 且 stop_hook_active=false
        │    （SubagentStop / 权限交互 / hook 递归 一律忽略）
        │ ④ 摘要：本地确定性清洗（§3.3）
        │ ⑤ 去重：SHA-256(session+"\n"+原文)，2 秒窗口（§3.4）
        │ ⑥ 写 payload → Start-Process 派生 show-popup.ps1（-STA -WindowStyle Hidden）
        │ ⑦ 清理 >10min 旧 payload → 写日志 → exit 0
        ▼
show-popup.ps1（独立 UI 进程）
        │ 读取 payload 后立即删除 → WPF(DirectWrite) 渲染
        ▼
右下角非模态卡片：白底圆角16 + 阴影 · ShowActivated=false（不抢焦点）
  淡入 → 停留 8 秒（hold）→ 淡出 · 点击卡片 / ✕ 立即关闭
```

### 3.2 Stop hook stdin 字段（实测抓包确认，v2.1.220）

```json
{
  "session_id": "uuid",
  "transcript_path": "C:\\Users\\<你的用户名>\\.claude\\projects\\<项目目录编码>\\<uuid>.jsonl",
  "cwd": "d:\\<当前项目工作目录>",
  "prompt_id": "uuid",
  "permission_mode": "default",
  "effort": { "level": "max" },
  "hook_event_name": "Stop",
  "stop_hook_active": false,
  "last_assistant_message": "本次回复全文（UTF-8）",
  "background_tasks": [],
  "session_crons": []
}
```

### 3.3 摘要清洗（notify-complete.ps1 的 Get-Summary，纯本地，不调模型）

顺序（审查修复后）：删代码块 ` ```...``` ` → 删行内代码 `` `...` `` → **删图片 `![..](..)`（必须先于 URL）** → 删 HTML 标签 `<...>` → 常见实体转回（`&amp;`→`&` 等）→ 删残留实体 `&[a-zA-Z#0-9]{1,8};` → 删标题 `(?m)^#+\s*`（multiline）→ 删列表 `(?m)^\s*[-*+]\s*`（multiline）→ 删 URL `https?://[^\s]+` → **不删 emoji**（WPF DirectWrite 渲染正常；原 surrogate 正则会误删所有非 BMP 字符）→ 取首段（空行分段）→ 压缩空白 → 截断 `$MAX_CHARS`（默认 180）字加 `…`。

### 3.4 启发式判断（Test-NeedInfo，第三轮审查修复后）

判断顺序（第一命中即返回）：
1. **skill 收尾标记**（格式即答案）：先删代码块（块内标记不生效），**只检查最后一个非空文本行**是否为行首标记（`^\s*❓ 需要你提供` / `^\s*✅ 已完成`）——较早的示例/说明行不覆盖后续真实请求
2. **礼貌性追问排除**：`需要我…吗？` → done（"需要我继续优化吗？"不再误判）
3. 兜底启发式（只分析最后一段）：结尾问号 `[?？]\s*$`；请求词 `请(你|您)?(提供|告诉我|告知|确认|补充|输入|上传|发送|给我)`；`需要你|需要您|麻烦你|请把|能否|你能不`

已知边界：引号内引用的历史请求词（如"已修复提示语'请提供密码'"）仍会命中——罕见场景，接受。

### 3.5 去重（规格 §4.6 落地，第四轮审查修复后）

- 键：`SHA-256(session_id + "\n" + 原文)`（原文未摘要化，保证同回复去重精确）
- state.json：`{"key":"...","ts":"ISO8601 UTC"}`；TTL：2000ms；时间差加 `>= 0` 防御时钟回拨
- **读-判-写整体包在命名 Mutex**（`Local\ClaudeCodeNotifyDedupe`，**WaitOne 100ms** 超时，不阻塞 500ms 目标）内串行化
- 未取得锁 → **fail-open**（不去重，记 `DEDUPE mutex-timeout` 便于区分正常 fail-open 与去重故障），绝不无锁读写 state.json
- `AbandonedMutexException` = 已取得锁（上一持有者崩溃），必须释放
- 写入：GUID 随机临时名 + `Move-Item -Force` 原子替换

### 3.5b 后台任务语义（第二轮审查修复后）

`background_tasks` 非空 → **不跳过也不误报**：标题显示「⏳ 主回复完成，后台任务运行中」（payload 带 `background=true`）。原因：后台任务结束后未必再次触发 Stop，跳过会导致永远没有通知。

### 3.6 payload 结构

```json
{ "emoji": "✅", "title": "任务完成", "body": "摘要", "project": "目录名", "background": false, "sessionId": "uuid", "createdAt": "ISO8601" }
```

### 3.7 WPF 渲染（show-popup.ps1，第二轮审查修复后）

- 单位全部为 DIP（= CSS px 语义，自动适配 150% DPI，与 HTML 调整器预览一致）
- 尺寸常量：W=400 H=200 MARGIN=20 PAD_X=20 PAD_TOP=15 BODY_TOP=52 BODY_H=80；字号 标题 12pt 正文 10pt（FontSize = pt×4/3）
- **窗口比卡片大 30px/边（SHADOW_PAD）**：阴影画在卡片外，必须留出透明边距否则被窗口裁剪（历史 bug）
- `AllowsTransparency=True`（圆角）+ `WindowStyle=None` + `ShowActivated=False` + **Show 后 `GetWindowLongPtr/SetWindowLongPtr` 叠加 `WS_EX_NOACTIVATE | WS_EX_TOOLWINDOW`，再用 `SWP_FRAMECHANGED|SWP_NOACTIVATE` 刷新样式**（第二轮审查修复：Ptr API、不移动不激活） + `Topmost=True`
- **多显示器定位（第二轮审查修复）**：`GetCursorPos → MonitorFromPoint → GetDpiForMonitor(目标屏)` 取**目标屏幕**的有效 DPI，`SetWindowPos` 按该屏物理像素定位右下角（`SWP_NOACTIVATE`）；不读当前窗口所在屏的 TransformToDevice（混合 DPI 会算错）
- 圆角 16、浅灰 1px 边框、DropShadowEffect（Blur24/Depth6/Opacity0.18）
- **布局为三列 Grid（第二轮审查修复）**：标题(*，TextTrimming 可收缩) / 项目名(Auto，MaxWidth=140 截断) / ✕(Auto 固定)——长标题+长项目名也不会覆盖关闭按钮
- 正文 TextBlock：Wrap + CharacterEllipsis + MaxHeight=BODY_H
- **动画（第三轮审查修复）**：hold 阶段用 `Stopwatch` 单调时钟判断 8 秒（DispatcherTimer 累计 tick 会把 8 秒拖成 15 秒；DateTime 受系统校时影响）。**语义：hold 8 秒 + 淡入淡出 ≈ 9-10 秒总生命周期**（第四轮起代码与文档均为 Stopwatch，无"真实时间戳"残留）
- **进程 DPI（第三轮审查修复）**：创建任何窗口前 `SetProcessDpiAwarenessContext(PER_MONITOR_AWARE_V2)`——否则进程仅 system-aware，`MDT_EFFECTIVE_DPI` 返回系统 DPI 而非目标屏 DPI
- **多显示器定位（第三轮+第五轮审查修复）**：单次 `GetCursorPos`（避免两次调用间鼠标跨屏）→ `MonitorFromPoint` → **同一 HMONITOR** 的 `GetMonitorInfo`（工作区）+ `GetDpiForMonitor`（DPI）→ `SetWindowPos` 物理像素定位（`SWP_NOACTIVATE`）；**失败全链路回退**：PMv2 失败 / GetCursorPos / MonitorFromPoint / GetMonitorInfo / GetDpiForMonitor 任一失败 → 跳过 SetWindowPos 保留 WPF 初始位置（绝不按 96 或未初始化数据计算——副屏 150% 下会缩小/错位）
- **Win32 调用失败检查（第三轮+第五轮审查修复）**：DllImport `SetLastError=true`；GetWindowLongPtr/SetWindowLongPtr 的失败判断收进 C# 包装层（调用前 `SetLastError(0)` + `Marshal.GetLastWin32Error()`——PS 5.1 层直接调会抛 RuntimeException；**返回 0 且 last error≠0 才是失败**，返回 0 且 last error=0 是"成功但旧值恰为 0"）；SetWindowPos/GetMonitorInfo/GetDpiForMonitor 返回值检查，失败写日志（`UI style-read-failed` / `UI style-apply-failed` / `UI monitor-info-failed` / `UI dpi-failed-initial-pos` / `UI position-failed`）不静默
- 关闭事件：窗体 MouseLeftButtonDown / ✕ → Close → `$app.Shutdown()`（无 Esc——无焦点窗口收不到键盘事件）
- **顶层兜底（审查修复）**：脚本级 `trap` → `UI fatal: <异常类型>` 日志 + exit 1
- **Payload 路径约束（第二轮审查修复）**：仅接受 `%TEMP%\claude-code-notify\payload-<32hex>.json`（GetFullPath 规范化后校验），防止误删任意 JSON；校验失败 `UI payload-rejected` + exit 1 且不删除文件

### 3.8 日志（脱敏）

**写入机制（第六轮+第八轮审查修复）**：入口与 UI 进程共写同一 notify.log，原 `Out-File -Append` 无锁 + 各自删除超限文件 → 并发时丢日志。现改为：命名 Mutex（`Local\ClaudeCodeNotifyLog`，WaitOne 100ms）串行化「检查→滚动→追加」，滚动用 `SetLength(0)` 截断（**不删除文件**——避免另一进程已打开句柄失效）。超时分支：**直接放弃本条，不无锁写也不重试**（AppendAllText 在持锁进程 ReadWrite 打开下会共享冲突抛异常，被 catch 吞等于静默丢日志；不重试是为控制入口最坏等待——去重锁 100ms + 日志锁 100ms ≈ 200ms，保证 500ms 返回目标成立）。

格式：`[yyyy-MM-dd HH:mm:ss] <事件>`，事件枚举：`OK adapter=wpf need_info=bool bg=bool dur=<ms> sid=<前8位>`（dur 为入口实际耗时，验证 500ms 目标）、`SKIP event=/stop_hook_active/`、`DEDUPE`、`DEDUPE mutex-timeout`、`ERR invalid-json/payload-write/spawn-ui`、`UI payload-ok/payload-rejected/payload-invalid/closed/render-error/fatal/pmv2-failed-initial-pos/pmv2-error-initial-pos/cursorpos-failed/monitor-failed/monitor-info-failed/dpi-failed-initial-pos/style-read-failed/style-apply-failed/style-refresh-failed/position-failed`。禁止记录：回复全文、环境变量、文件内容。

---

## 四、关键配置与代码要点

### 4.1 settings.json 的 hooks 节（当前生效，审查修复后）

```json
{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "\"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe\" -NoProfile -WindowStyle Hidden -ExecutionPolicy Bypass -File \"C:\\Users\\<你的用户名>\\.claude\\scripts\\notify-complete.ps1\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
```

- **绝对解释器路径**（防工作目录/PATH 劫持）；**`-WindowStyle Hidden`**（消除每次 Stop 闪现 powershell 窗口的问题）

### 4.2 派生命令（notify-complete.ps1 内，审查修复后）

```powershell
# UI 脚本路径用 $PSScriptRoot（可移植）；解释器用 $env:WINDIR 绝对路径
# PS 5.1 的 -ArgumentList 数组会拼接为字符串且不含空格引号 → 用字符串形式手动加引号
Start-Process -FilePath $PS_EXE -ArgumentList (
    '-NoProfile -STA -WindowStyle Hidden -ExecutionPolicy Bypass -File "' + $SHOW_POPUP +
    '" -PayloadFile "' + $payloadFile + '"'
) -WindowStyle Hidden | Out-Null
```

派生失败（catch）→ 记 `ERR spawn-ui` 并**立即删除 payload**，避免残留（审查修复）。

### 4.3 已知坑与规避（实现过程中踩过并已修复）

| 坑 | 规避 |
|---|---|
| PS 5.1 无 BOM 的 UTF-8 脚本按 ANSI/GBK 解释 → 中文乱码 | 两脚本 UTF-8 with BOM（`[IO.File]::WriteAllText(..., [Text.Encoding]::UTF8)` 补写） |
| PS 5.1 读 stdin 中文乱码 | 脚本开头 `[Console]::InputEncoding = UTF8` |
| PS 5.1 `New-Object TypeName(a, b)` 参数解析坑（Object[] 减法错） | 一律 `[Type]::new(...)`（WPF 重写后已无此问题） |
| `CreateRoundRectRgn` 在 gdi32.dll 而非 user32.dll | WPF 重写后已无 P/Invoke（CornerRadius 原生） |
| `Control.CreateHandle()` 是 protected | WPF 重写后已无此问题 |
| WPF 必须 STA | 入口派生时带 `-STA` |
| 阴影被窗口边界裁剪 | 窗口加 SHADOW_PAD 透明边距 |
| 函数中使用后定义的变量（`$MAX_CHARS` 在 Get-Summary 调用后才赋值 → 正文只剩 `…`） | 常量定义移到函数前 |
| GDI 小字号渲染发糊 | 整体重写为 WPF（DirectWrite） |
| `$app.Run()` 返回退出码污染 stdout | `$null = $app.Run()` |
| Git Bash `printf`/heredoc 会把 `\\` 折叠为 `\`（仅测试链路） | 测试用 PS 内部 `ConvertTo-Json` 或文件方式 |
| 真实 hook 的 JSON 由 Node 序列化（UTF-8、`\\` 正确转义） | 无此问题 |
| **hook 裸 powershell + 无 Hidden → 每次 Stop 闪现控制台窗口 + PATH 劫持面**（GPT 审查 P1） | command 改绝对解释器路径 + `-WindowStyle Hidden` |
| **ShowActivated=False 不够，点击仍可能激活**（GPT P2） | Show 后叠加 `WS_EX_NOACTIVATE \| WS_EX_TOOLWINDOW` |
| **固定主屏定位，副屏/混合 DPI 不符**（GPT P2） | `SetWindowPos` 按鼠标屏幕物理像素定位 |
| **全文搜索请求词 → "请检查结果"误判**（GPT P2） | 只查最后一段 + 收紧正则（回归测试通过） |
| **URL 先删导致图片残留 `![x](`；surrogate 误删非 emoji；^ 无 multiline**（GPT P2） | 图片先删/保留 emoji/(?m) |
| **去重固定 .tmp 并发踩踏**（GPT P3） | GUID 随机临时名 |
| **UI 初始化无兜底、派生失败 payload 残留**（GPT P3） | 脚本级 trap + catch 删 payload |
| **长项目名覆盖 ✕**（GPT P3） | MaxWidth=140 + CharacterEllipsis |
| **8 秒实际 15 秒**（GPT 第二轮 P2） | hold 改 Stopwatch 单调时钟（实测 9-10s） |
| **混合 DPI 用错屏幕 scale**（GPT 第二轮 P2） | GetDpiForMonitor 取目标屏 DPI |
| **WS_EX 应用不完整**（GPT 第二轮 P2） | Get/SetWindowLongPtr + SWP_FRAMECHANGED\|NOACTIVATE |
| **background_tasks 跳过致永久无通知**（GPT 第二轮 P2） | 不跳过，标题改「主回复完成，后台任务运行中」 |
| **礼貌性追问误判**（GPT 第二轮 P2） | skill 标记优先 + `需要我…吗？` 排除（回归通过） |
| **去重检查-写入竞态**（GPT 第二轮 P3） | 命名 Mutex 串行化读-判-写 |
| **普通链接残留 `[报告](`**（GPT 第二轮 P3） | `[text](url)` 先替换为 text 再删 URL |
| **长标题+项目名覆盖 ✕**（GPT 第二轮 P3） | 三列 Grid（标题* / 项目名 Auto / ✕ Auto） |
| **PayloadFile 无路径约束**（GPT 第二轮 P3） | 校验 `%TEMP%\claude-code-notify\payload-<32hex>.json` |
| **文档与代码漂移**（GPT 第二轮 P3） | Esc 处理器已从代码删除，文档同步 |

### 4.4 已知边界（规格中未实现/刻意不做）

- **Toast 原生通知**：不做（WinRT AppUserModelID 环境差异大，纯 WPF 已满足）
- **单击弹窗激活原终端**：不做（需反向定位终端窗口）
- **quietHours/配置文件化**：不做（参数直接硬编码在 show-popup.ps1 顶部，用 HTML 调整器改）
- **多弹窗纵向排列**：不做（同位置覆盖，单用户场景概率低）
- **系统"勿扰"模式**：不检测（WPF 弹窗不理会）
- **notification.json 项目级配置**：未实现

---

## 五、验证记录（2026-08-07 实测）

| 项 | 结果 |
|---|---|
| 入口 500ms 内返回 | ✓（日志 `OK` 与 `UI payload-ok` 间隔 <1s） |
| 真实端到端（`claude -p`） | ✓ 完整链路：OK → payload-ok → closed（hold 8s；`claude -p` 一次性会话中 Job 清理会提前终止 UI，交互式会话完整） |
| stop_hook_active=true 过滤 | ✓ `SKIP stop_hook_active` |
| SubagentStop 过滤 | ✓ `SKIP event=SubagentStop` |
| 2s 去重（800ms 重复） | ✓ `DEDUPE` |
| 项目名渲染 | ✓ vision 确认「· my-project」 |
| ✕ 按钮 | ✓ vision 确认右上角 |
| 阴影/圆角 | ✓ vision 确认（修复裁剪后） |
| 中文/emoji/URL 摘要清洗 | ✓ 单测通过 |
| 异常兜底（无效 JSON → exit 0） | ✓ `ERR invalid-json` 不破坏主流程 |
| GPT 误判案例回归（"请检查结果"等 3 例） | ✓ 修复后 C1=False（不再误判）、C4=True（真请求识别） |
| 图片/URL 顺序清洗 | ✓ `![x](url)` 整段删除无残留 |
| 补充平面字符保留 | ✓ 不再误删非 emoji 字符 |
| multiline 标题/列表清理 | ✓ |
| 修复后端到端（claude -p，绝对路径+Hidden command） | ✓ 全链路 OK，且无 powershell 窗口闪现 |
| 入口耗时（dur 日志） | ✓ 253ms / 215ms < 500ms |
| 8 秒计时（Stopwatch 单调时钟） | ✓ 实测 payload-ok → closed ≈ 9-10s（启动开销另计） |
| 礼貌追问排除（"需要我继续优化吗？"） | ✓ False（不再误判） |
| skill 标记优先（✅ 已完成 / ❓ 需要你提供） | ✓ 格式即答案，直接命中 |
| 普通链接替换（[报告](url) → 报告） | ✓ 无残留破损语法 |
| Mutex 去重（命名互斥串行读-判-写） | ✓ 语法/运行正常 |

---

## 六、给审查者的重点问题清单（已按两轮审查修复更新）

1. **安全**：payload 已加路径约束（仅 `%TEMP%\claude-code-notify\payload-<32hex>.json`，校验失败不删文件）；解释器已用绝对路径（防 PATH 劫持）——剩余：临时目录为固定路径（普通用户可写，但写入内容可控）；**payload 的 body 为回复摘要，可能含项目名/错误信息片段，读后即删 + 10min 过期清理兜底**
2. **注入**：WPF TextBlock 非 HTML（无 XSS 面）；派生参数为固定脚本路径 + Guid 文件名（无用户输入进入命令行）
3. **并发**：去重读-判-写与日志写入/滚动均用命名 Mutex 串行化（去重锁超时 fail-open 不去重；日志锁超时直接放弃本条，不无锁写）；payload 为独立文件无共享写；日志滚动用 SetLength(0) 截断不删除（避免并发打开句柄失效）
4. **性能**：**500ms 为正常路径目标**（实测 205-275ms）；极端锁竞争下入口最坏 ≈ 400ms（去重锁 100ms + 日志锁 100ms + 冷启动 ~200ms），远低于 hook timeout 5s；UI 进程约 10 秒生命周期（8s hold + 淡入淡出），频率低（每次任务结束一次）
5. **编码**：Node(UTF-8) → stdin(InputEncoding=UTF8) → 内存（无中间文件）；transcript 兜底用 `Get-Content -Encoding UTF8`——无已知乱码场景
6. **DPI**：目标屏 DPI 用 `GetDpiForMonitor`（混合 DPI 已修）；WPF DIP 尺寸与 150% 主屏的 HTML 预览一致——剩余边界：副屏缩放不同时，WPF 窗口 DIP 尺寸按目标屏 scale 换算物理尺寸（已按目标屏算），跨屏拖拽由系统处理
7. **hook 语义**：`timeout: 5` 实测入口 253ms，含 transcript 兜底（-Tail 80）无压力
8. **隐私**：`last_assistant_message` 只在内存处理、payload 读后即删（含 10min 过期清理与派生失败即删）；日志只记事件/会话前 8 位/耗时

**新确认边界**：`claude -p` 一次性会话退出时，Claude Code 的进程 Job 会清理 hook 派生的 UI 进程（实测弹窗 2 秒被终止）——**交互式会话（正常使用形态）不受影响**，弹窗完整显示 8 秒。
